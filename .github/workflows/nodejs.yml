name: Node.js CI
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@master
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@master
        with:
          node-version: ${{ matrix.node-version }}
      - name: yarn install, build
        run: |
          cd ./functions
          npm install

  deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    name: Deploy
    needs: build
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Build each site
        run: |
          cd ./functions
          npm install

      - name: Ready to deploy
        run: |
          cp env-sample .env
          echo "${{ secrets.GLAMPI_API_KEY }}" > .env
          yarn global add firebase-tools
          yarn
          yarn generate
          cd functions
          npm install

      - name: Deploy to Firebase
        run: |
          firebase deploy --only hosting:glampy --token $FIREBASE_TOKEN
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

      - name: Slack notification
        uses: Ilshidur/action-slack@master
        with:
          args: 'https://glampy.web.app/'
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_CHANNEL: deploy-glampi # Optional.
          SLACK_OVERRIDE_MESSAGE: 'glampi Deploy' # Optional.
